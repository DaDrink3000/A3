<h1>Set up MFA</h1>
<% if (typeof err === 'string' && err) { %>
  <p class="small" style="color:#b00020;"><%= err %></p>
<% } %>
<p>Scan this QR code with Google Authenticator / Authy, then enter the 6-digit code.</p>
<% if (qr) { %><img src="<%= qr %>" alt="MFA QR" /><% } %>
<p class="small muted">Or manually enter secret: <code><%= secret %></code></p>
<form method="POST" action="/auth/setup-mfa">
  <input type="hidden" name="_csrf" value="<%= csrfToken %>"/>
  <label>Authenticator Code</label><br/>
  <input type="text" name="token" pattern="\d{6}" required placeholder="123456"/><br/><br/>
  <div class="actions"><button class="primary" type="submit">Enable MFA</button></div>
</form>
